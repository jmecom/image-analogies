function [best_app_i, best_app_j] = best_approximate_match(A_features, ...
  B_features, l, i, j)
%BEST_APPROXIMATE_MATCH ...

global N_BIG;
global B;

% Convert (i,j) back...
i = i - floor(N_BIG/2);
j = j - floor(N_BIG/2);

anno = ann(A_features{l});

size(A_features{l}(1,:))
display('----')
query+(B_features{l}(sub2ind(size(B(:,:,1)), i, j));
[idx, ~] = ksearch(anno, B_features{l}(i,j), 1, 1.0);

idx

anno = close(anno);

end

